export class User {
    constructor(username, password, firstName, lastName, id){
        this.username = username;
        this.password = password;
        this.firstName = firstName;
        this.lastName = lastName;
        this.id = id;
    }
}